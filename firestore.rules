rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /download_leads/{leadId} {
      // Allow Cloud Functions and server integration to manage download_leads
      allow read, write: if true;
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
